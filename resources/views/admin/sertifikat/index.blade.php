@extends('layouts.admin') @section('page-title', 'Template Sertifikat') @section('content')
<div class="flex items-center justify-between mb-6"><h3 class="text-lg font-semibold text-on-surface">Template Sertifikat</h3><a href="{{ route('admin.cert-template.create') }}" class="inline-flex items-center gap-2 bg-primary text-on-primary px-4 py-2 rounded-xl text-sm font-bold"><i class="fa-solid fa-plus text-sm"></i> Tambah</a></div>
<div class="grid grid-cols-1 sm:grid-cols-2 gap-4">@foreach($templates as $t)<div class="glass-card rounded-2xl border border-white/5 overflow-hidden"><img src="{{ asset('storage/' . $t->front_image) }}" class="w-full h-32 object-cover"><div class="p-4"><p class="font-medium text-on-surface mb-1">{{ $t->name }}</p><p class="text-xs text-on-surface-variant">{{ $t->training->title }}</p><div class="mt-2 flex items-center gap-3"><a href="{{ route('admin.cert-template.edit', $t) }}" class="text-primary text-xs hover:underline">Edit</a><form method="POST" action="{{ route('admin.cert-template.destroy', $t) }}" onsubmit="return confirm('Hapus?')">@csrf @method('DELETE')<button class="text-error text-xs hover:underline">Hapus</button></form></div></div></div>@endforeach</div>
<div class="mt-4">{{ $templates->links() }}</div>
@endsection
